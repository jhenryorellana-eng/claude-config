# Archivo — bitácora visual por fase de UsaLatinoPrime V2 (F0 → F4-Ola3)

> Curaduría agent-ops 2026-08-13. Verbatim, movido desde `MEMORY.md` (tope de 200
> líneas cargadas). El MEMORY.md conserva el sistema visual y las reglas de dirección
> de arte destiladas.
>
> Aviso: buena parte de estas entradas mezcla dirección de arte con detalle de
> IMPLEMENTACIÓN (props de componentes, boundaries de eslint, scripts de i18n, harness
> de Playwright). Si `frontend-builder` necesita ese detalle, este archivo es la fuente;
> no duplicarlo en su MEMORY.

---

### UsaLatinoPrime V2 — Design System F0 (Next.js 15 + Tailwind v4 + shadcn, jun 2026)
- **Stack repo**: `usalatino-v2/` Next 15.5.19 (App Router, Turbopack), React 19.1, Tailwind v4, TS strict, `@/* -> ./src/*`. Boundaries lint con `eslint-plugin-boundaries` (R2: `src/frontend/` NUNCA importa `src/backend/`). Para que `import "./globals.css"` no dispare `app→app`, agregar `settings["boundaries/ignore"]: ["**/*.css"]` (los imports CSS side-effect no son deps arquitectónicas).
- **Marca UsaLatinoPrime** (Utah flag): navy `#002855` + blue `#2F6BFF` + gold `#FFC629` + green `#1BB673` + red `#E4002B`. Gradiente marca `linear-gradient(125deg, #2F6BFF, #FFC629)`. Font display = **Plus Jakarta Sans** (next/font/google).
- **Plus Jakarta Sans NO tiene peso 900** — Google Fonts/next-font lo tipa 200–800. Si un doc pide 900, usar 800 (clamp) y documentar TODO(SoT). `weight:["600","700","800"]`.
- **Dos superficies bajo un solo switch `[data-theme]`**: móvil (app cliente, default) y desktop staff (scope `.surface-staff` que sobre-escribe los mismos tokens). Tokens semánticos light/dark en `tokens.css` bajo `[data-theme="light"|"dark"]` y `[data-theme].surface-staff`.
- **shadcn + tokens de marca sin pelearse**: el `@theme inline` de tokens.css ya mapea `--color-bg/card/accent/...`. En `shadcn-theme.css` mapear SOLO los nombres shadcn-específicos (`primary`, `*-foreground`, `popover`, `destructive`, `border`, `input`, `ring`, charts, sidebar) para no colisionar `--color-accent`/`--color-card` (gana el último @theme). El `accent` de shadcn (tint sutil de hover ghost/outline) ≠ accent de marca: dejar `bg-accent`=tint sutil y exponer la acción de marca como `bg-action` (`--color-action: var(--accent)`).
- **shadcn driven por `[data-theme]` no `.dark`**: `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));` en globals.css.
- **shadcn init flaky**: `npx shadcn@latest init` a veces NO instala runtime deps ni crea `lib/utils.ts`. Verificar e `npm install clsx tailwind-merge class-variance-authority lucide-react` + `tw-animate-css` (dev) a mano; crear `cn()` en utils. components.json: `style:new-york`, `baseColor:neutral`, `cssVariables:true`, `tailwind.config:""`, aliases a `@/frontend/components`.
- **No-flash theme**: script inline en `<head>` de layout.tsx (lee localStorage `ulp-theme`/`ulp-text-scale` antes de hidratar, fallback prefers-color-scheme) + `suppressHydrationWarning` en `<html>`. Escalas de texto 0.92/1/1.12 vía `[data-text-scale]` + `html{font-size:calc(100%*var(--text-scale))}`.
- **ProgressRing**: usar `React.useId()` para el id del `<linearGradient>` (evita colisión SSR con múltiples rings).
- **Optimizar GIF pesado → WebP animado con `sharp`** (sin gifsicle/imagemagick reales en Windows; `convert.exe` del sistema NO es ImageMagick): `sharp(src,{animated:true}).resize({width}).webp({quality,effort:4,loop:0}).toFile(out)`. Lex 9.5MB GIF (150 frames, 400px) → q40/w272 = 0.977MB (<1MB RNF). 150 frames a effort 6 tarda minutos; usar effort 4. Componente Lex usa `<picture><source webp><img gif></picture>`.
- **axe contraste**: los pares token-soft de la marca (StatusPill green/green-soft 2.37, gold-deep/gold-soft 1.93, accent/blue-soft 3.96) fallan 4.5:1 — son NORMATIVOS del doc + mitigados con icono+texto (§8.4). NO cambiar valores de marca; flaggear `<<NEED-A11Y-FIX>>` al dueño. Sí corregir el "chrome" propio del showcase (labels ink-3 → ink-2).
- **Hydration warning de Radix Switch**: `SwitchBubbleInput` (input oculto) + inyección `caret-color:transparent` de extensión del browser produce 1 console-error de hydration-mismatch SOLO en light en la primera navegación. Artefacto ambiental (la propia warning lo dice), NO bug — dark/staff/mobile dan 0. Filtrar `/hydrat|caret-color|extension/i` en el harness.

### UsaLatinoPrime V2 — F1 Shell staff + componentes desktop (DOC-01 §5.3)
- **Route group split para shell con/sin sidebar**: `(staff)/layout.tsx` = wrapper fino que aplica `.surface-staff` + `background:var(--bg)` a TODO (auth + panel). `(staff)/(panel)/layout.tsx` = shell real (Sidebar+Topbar). Auth pages (`login`/`reset-password`/`cambiar-password`) quedan directo bajo `(staff)/` → sin sidebar. Los route groups `(x)` NO afectan la URL. Mover carpetas con `mv` (git lo ve como D+nuevo). **OJO**: tras mover rutas, `rm -rf .next` para limpiar `.next/types/validator.ts` stale (referencia paths viejos → TS2307 fantasma).
- **next-intl v4 typed keys + claves dinámicas**: `getTranslations(ns)` tipa cada call contra el set literal de keys de es.json. Para keys data-driven (`groups.${labelKey}`, `roles.${actor.role}`) castear el translator: `const tNav = tNavRaw as unknown as (key:string)=>string`. Es el escape estándar; no rompe la paridad (la valida `check:i18n`).
- **Sidebar staff (prototipo Vanessa) — lenguaje visual EXACTO**: brand mark "U" 38px gradiente `navy→accent` + `anim-sheen`; "USALATINO**PRIME**" (PRIME en `--gold-deep` light); subtítulo panel uppercase 10.5px/800 `--ink-3`. Grupos con `nav-group-label` (mismo 10.5px/800 uppercase tracking .7px). Nav item activo = `linear-gradient(135deg, var(--accent), color-mix(--brand-navy 70%, --accent))` texto blanco + sombra accent; hover (solo inactivos) `--hover` via `.nav-item:not([data-active]):hover`. Badge rojo numérico minWidth 20. Footer user-chip: Avatar staff + nombre/título + logout slot. 248px (`--sb-w`), `position:sticky;top:0;height:100dvh`.
- **Componentes desktop construidos (kebab-case file, PascalCase export)**: `kpi` (icono 40px IconTile, valor 32px/900, trend ↑↓, variante `hot`=gradiente accent→navy+glow+radial), `data-table` (genérica `<T>`, sticky header 12px/800 uppercase ink-3, sort por columna controlado, rows hover `--hover` via `.dt-row:hover`, "Cargar más" cursor — NUNCA offset, skeleton rows en loading), `empty-state` (Lex calma/atento + título + sub + CTA GradientBtn + secondary GhostBtn + code colapsable), `modal` (Radix Dialog, radio 20px, scrim `rgba(7,17,33,.5)`+blur, rail accent en título, apilable porque cada uno portaliza), `side-panel` (Radix Dialog 400px right-docked, `slideInRight .28s`, header+scroll body+footer pinned), `switch` (Radix 44×25, on=verde via `.staff-switch[data-state=checked]` CSS — inline no puede targetear data-state), `skeleton` (`--chip` block + `shimmer` keyframe), `toast` (sonner + `BrandToaster` que observa `data-theme` con MutationObserver porque la app NO usa next-themes; re-exporta `toast`), `topbar` (search button-as-input ⌘K, ThemeToggle, bell, user-chip+logout), `sidebar`.
- **Reglas CSS de estado (inline no alcanza)**: cuando un componente Radix necesita estilos por `[data-state]` o `:hover`/`:focus-visible`, agregarlos a `motion.css` (ya global) con clase propia (`.staff-switch`, `.dt-row`, `.nav-item`, `.side-panel-content[data-state=open]`). Inline styles no targetean pseudo/data-attrs.
- **Shell con server action inyectada**: layout server (RSC) construye Actor vía `@/backend/modules/identity` (index re-exporta `getActor`), filtra nav por permisos (`filterNav` + `actor.permissions.get(module)`, admin bypass), resuelve staff profile, y pasa `logoutAction` (server action) como prop al `StaffShell` client. El client envuelve en `useTransition(()=>logoutAction())`. Patrón DOC-50 §2 inject-actions.
- **Lecturas para el shell agregadas a identity (módulo dueño de auth/staff)**: `getStaffProfileById` (server client, RLS lee propio perfil), `countActiveStaff`/`countActiveEmployees` (`{count:'exact',head:true}`), `getCurrentStaffProfile`. Exportadas desde index. KPIs sin módulo aún (casos/servicios) = `null` → render em-dash `—` con `TODO(F1-W2)`, NUNCA cero falso.
- **Trabajo en paralelo con otro agente (catalog/audit)**: cuando otro agente construye módulos en paralelo, `git status` muestra sus carpetas como `??` (untracked). `tsc --noEmit` global mostrará SUS errores (catalog/audit/sus adiciones a identity/actions). Verificar SOLO los propios con `grep -E "<mis-paths>"`. NO tocar sus archivos. Pueden co-editar el mismo archivo (identity index/service/repository/actions): los símbolos propios sobreviven si no colisionan nombres.
- **Playwright sin login (panel auth-gated)**: agregar sección "Desktop" al showcase `/design` (ruta pública en middleware) con todos los componentes nuevos → sirve de demo + QA visual. Harness `scripts/shoot-desktop*.cjs` (playwright local): `addInitScript` set `localStorage.ulp-theme` antes de hidratar, click botón `[aria-pressed]` hasText "Superficie" para activar `.surface-staff`, `scrollIntoViewIfNeeded` al heading, screenshot. Modal/SidePanel: click trigger → `waitForTimeout(500)` → screenshot → `keyboard.press("Escape")`. 0 console errors en light Y dark/staff.

### UsaLatinoPrime V2 — F1 Admin screens (DOC-53, jun 2026)
- **Boundaries clave**: `app` puede importar `module-pub` (= `index.ts` Y `actions.ts` por el glob `*/(index|actions).ts`). `frontend` SOLO importa `frontend`+`shared` — NUNCA actions. Patrón obligatorio: **server page (RSC) lee vía index.ts + pasa actions como props** a client feature components (inject-actions, DOC-50 §2). Las actions del módulo (`catalog/actions.ts`, `identity/actions.ts`) NO llevan `"use server"` (catalog) — se envuelven en thin wrappers `"use server"` en `src/app/.../actions.ts` que el server page importa y pasa como prop. `identity/actions.ts` SÍ tiene `"use server"` (se puede pasar directo).
- **Catalog reads faltantes que tuve que crear** en `catalog/service.ts` + exportar en index: `listServicesAdmin(actor)` (lista admin incl. archivados + planCount/phaseCount, `can(catalog,view)`), `getServiceEditorTree(actor, serviceId)` (service+plans+phases{milestones,policy,docs,forms}). El `getPublicCatalog` solo devuelve públicos/activos — inútil para el panel admin. `listServicesForEditor` (repo) ya existía pero sin wrapper de servicio.
- **Módulo `org` NUEVO** (cohesión: org settings + cover_templates + terms_versions = config organizacional consumida por app cliente/emails, distinta de identity=auth/staff). Tablas YA EXISTEN en DB (P-1 de DOC-14 RESUELTA): `orgs.settings` jsonb, `cover_templates`, `terms_versions(version, title_i18n, body_md_i18n, is_active, published_at)`. Actions creadas: `updateOrgSettingsAction` (P-53-2a), `setCoverTemplateActiveAction` (P-53-2b parcial), `publishTermsVersionAction`+`createTermsVersionAction` (P-53-2c — P-1 resuelta). API-IDs propuestos API-ORG-01…05.
- **`--purple` token NO existía** — DOC-53 §7 pide "Finanzas púrpura" + el color enum del catálogo incluye purple. Agregué `--purple`/`--purple-soft` a tokens.css en AMBOS scopes staff (light `#7c5cff`/soft, dark `#9d86ff`). Es normativo del doc, no invención.
- **EmployeeRow** (identity): `{userId,email,isActive,displayName,role,titleI18n:Json|null,avatarUrl,permissions:[{module_key,can_view,can_edit}]}`. role ∈ admin/sales/paralegal/finance. `listEmployeesAction()` ya existe (`"use server"`).
- **Matriz permisos UI (RF-ADM-045)**: 20 módulos (MODULE_KEYS). Regla encadenada cliente: Editar→fuerza Ver; apagar Ver→apaga Editar. Filas admin deshabilitadas. Guardar = `updateEmployeePermissionsAction({staffId,permissions})` → toast inmediato. Preset por rol vive en backend (`buildPermissionPreset`) — NO exportado; para el modal "nuevo empleado" repliqué la matriz DOC-22 §6 en `shared/constants` o pasé preset desde server.
- **PublicationCheck** (gate publicación servicio): `{ok:boolean, issues:[{code,severity:'blocking'|'warning',ref?,detail}]}`. `activateServiceAction(id)` devuelve el check; si `!ok` NO publica, muestra issues exactos (CATALOG_NO_ACTIVE_PLAN, CATALOG_NO_PHASES, CATALOG_I18N_INCOMPLETE, CATALOG_EXPLAINER_MISSING warning). Los `detail` ya vienen en español del domain.
- **Dev preview para Playwright** (panel auth-gated): ruta `(dev)/admin-preview/[view]/page.tsx` pública (agregar prefix `/admin-preview` a middleware PUBLIC_PATH_PREFIXES) que renderiza cada vista con datos mock + `notFound()` si `process.env.NODE_ENV==='production'`. Envolver en `.surface-staff`. Harness `scripts/shoot-admin*.cjs`: set `localStorage.ulp-theme` antes de hidratar, screenshot light+dark.
- **DataTable desktop** (`@/frontend/components/desktop`): genérica `<T>`, props `columns/rows/rowKey/onRowClick/sort/onSortChange/hasMore/onLoadMore/loading/empty`. Modal/SidePanel = Radix Dialog controlados (`open/onOpenChange/title/footer`). Switch desktop = Radix 44×25 on=green. EmptyState con Lex mood calma/atento + action/secondaryAction/code. Kpi con `hot`. Todos style inline + tokens.
- **react-hook-form NO instalado**: usé estado controlado simple (useState) en wizard/forms — suficiente, evita dependencia nueva. Editor i18n ES|EN lado a lado = patrón propio (dos inputs + chip "Falta EN" cuando EN vacío).
- **i18n**: claves bajo `staff.admin.*` en es.json + paridad exacta en.json (`npm run check:i18n`). El checker hace flatten + diff de keys; values pueden diferir pero TODA key ES debe existir en EN. Truco para merge bilingüe sin romper paridad: script Node con objeto `{leaf:[es,en]}` + `pick(node,idx)` recursivo → escribe ambos JSON de una. OJO: heredoc bash rompe con emoji/apóstrofes — escribir el script como archivo con Write, no con `cat <<EOF`.
- **terms_versions + cover_templates + orgs.settings YA EXISTEN en la DB** (P-1 DOC-14 resuelta). Shapes: terms_versions{version,title_i18n,body_md_i18n,is_active,published_at}, cover_templates{name,template:jsonb,is_active}. contract_terms_acceptances{terms_version:text,case_id,user_id,accepted_at} para compliance count. Configuración §9 es 100% implementable en F1 (NO bloqueada).
- **eslint ignora docs/**** — los harness `.cjs` Playwright en docs/_evidence usan `require()` → no-require-imports. Agregué `"docs/**"` a `ignores` de eslint.config.mjs (NO toqué scripts/ que es off-limits del otro agente).
- **Playwright harness sin login para panel admin auth-gated**: NO usar `/tmp` (require('playwright') no resuelve desde temp). Poner el `.cjs` DENTRO del proyecto (docs/_evidence/) para que resuelva node_modules local. Ruta dev `(dev)/admin-preview/[view]` + middleware prefix `/admin-preview` + `notFound()` si NODE_ENV==='production'. `addInitScript` set localStorage ulp-theme ANTES de hidratar + `page.evaluate` set `data-theme` en documentElement DESPUÉS de cargar (el script no-flash lo lee de localStorage). 1440×900 desktop, 390×844 mobile. Filtrar `/hydrat|caret-color|extension/`. 11 screenshots, 0 console errors light+dark.
- **Dev indicator de Next 15 "N" badge** aparece bottom-left en screenshots dev (igual que astro-dev-toolbar). Es artefacto dev, no UI. No molesta para evidencia.
- **Resultado F1 admin (5 vistas)**: dashboard (KPIs reales: empleados+servicios activos vía listServicesAdmin; casos=null em-dash), catálogo (lista grid + wizard 6 pasos, paso 5 formularios=stub F4 con Lex señala), empleados (tabla+matriz 20×2 switches+modal crear 2 pasos+desactivar), auditoría (tabla+diff viewer SidePanel 3-col red-soft/green-soft+i18n por idioma+raw JSON+CSV export Blob), configuración (3 tabs: general form tipado, carátulas grid+switch, T&C versiones+publicar+compliance). 4 gates verdes: tsc 0, eslint 0, check:i18n OK(391), vitest 199/199.

### UsaLatinoPrime V2 — F2 Componentes móviles cliente (DOC-01 §5.2, jun 2026)
- **Prototipo es la SoT visual EXACTA**: `V2/UI Cliente/app/*.jsx`. `ui.jsx` tiene Icon(49)/Lex/GradientBtn/GhostBtn/Card/StatusPill/ProgressRing/Bar/Confetti/playChime/ScreenHead/NavBar+CaseNav+AccountNav/MessagingLauncher/LexFab/Scrim. `screens1.jsx`=Tutorial+CaminoScreen+Page+BrandBar; `screens8.jsx`=SignaturePad+DisclaimerScreen; `screens7.jsx`=MessagingSheet (bottom sheet 92% con header+thread+input+roster+CallOverlay); `screens2.jsx`=CelebrateScreen(Confetti+playChime). **app.jsx** define el chrome: `NO_CHROME={welcome,no-access,phone,otp,subir,exito,disclaimer}`, `ACCOUNT_SCREENS`, resto=CASO. AccountNav 5 tabs (Servicios/Mis casos/Comunidad/Avisos+badge/Pagos), CaseNav 5 tabs (Inicio/Citas/Documentos/Formularios/Más).
- **Componentes construidos** en `src/frontend/components/mobile/` (kebab file, PascalCase): `screen-head` (back 44px card+sombra + eyebrow/h1/sub + Lex 62px atento), `bottom-nav` (UN componente, prop `variant:'cuenta'|'caso'`, items+hrefs internos, activo=`usePathname` match o `activeOverride` para showcase, pill `blue-soft`+accent, badge rojo anillo 2px card, `nav-bg`+blur 16px, prop `absolute` para phone-frame), `bottom-sheet` (Scrim `rgba(7,17,33,.5)` fadeIn + sheet `slideUp .32s` radio top 28px + **drag-to-close real**: pointer en handle, dismiss >120px o velocity>0.6, spring-back; Escape+focus mgmt), `confetti` (port verbatim 150 partículas rAF 3200ms, lee colores de CSS vars en runtime, + `playChime` 659/784/1047Hz, reduce-motion N=0/silencio), `tutorial` (coach-marks, spotlight `spotPulse` con boxShadow `0 0 0 9999px`, Lex 92px señala, dots dorados, steps como prop con `targetRef`+`containerRef` para medir), `signature-pad` (tabs Dibujar/Subir, canvas 168px touch-action:none, +**undo por strokes** y `getDataUrl()` PNG vía forwardRef — el doc pide clear/undo/dataURL que el prototipo no tenía), `fab` (MessagingLauncher pill left+badge "Tu equipo" + LexFab 64px ring dorado). Barrel `index.ts`.
- **Chrome integration**: `(cliente)/layout.tsx` (server) resuelve nav labels via `getTranslations('cliente.nav')` y monta `AccountChrome` (client) que muestra AccountNav SOLO en rutas account (`usePathname` match `/home|/servicios|/comunidad|/avisos|/pagos|/config`) — mirror de la tabla de chrome del prototipo, porque un server layout no puede leer pathname. `caso/[caseId]/layout.tsx` (server) = guard `getActor` kind=='client' + header con `#ULP-...` (placeholder, `TODO(F2-W2)` para CaseWorkspaceDto del módulo cases) + `CaseChrome` client (CaseNav variant 'caso' + launcher). Ambos navs/FABs con `absolute` para anclar al marco 430px.
- **Módulo `cases` lo construye OTRO agente en paralelo** (untracked `??` en git): `requireCaseAccess` NO existe aún en identity index (solo `getActor/requireActor/can/systemActor/AuthzError`). El guard del caso usa kind=='client' + `TODO(F2-W2)` y NO bloquea (la membership la enforça RLS + el módulo cuando exista). `tsc --noEmit` global muestra ~14 errores SUYOS (cases/contracts repository `.Insert/.Update` no en tipos, catalog `getCatalogFirstPhase`) — verificar SOLO los propios con `grep -E "components/mobile|\(cliente\)|\(dev\)/design"` (= 0). NO tocar sus carpetas.
- **Motion utility classes agregadas a motion.css** (los keyframes ya existían): `.anim-fade-in` (.25s scrim), `.anim-slide-up` (.32s cubic-bezier), `.anim-check-pop`, `.anim-soft-pop`, `.anim-spot-pulse`, `.anim-glow-breath-fast` (1.2s dot vivo banner comunidad), `.anim-float-hint` (1.8s hint disclaimer). Agregadas también al bloque `prefers-reduced-motion`.
- **Showcase Mobile** = sección en `(dev)/design/page.tsx` (ruta pública en middleware). BottomNav en showcase NO matchea pathname `/design` → agregué prop `activeOverride` para forzar tab activo en preview. Overlays (BottomSheet, Tutorial) viven DENTRO de un contenedor `position:relative; overflow:hidden` para que su `absolute inset:0` se scopee/sea visible (Tutorial bubble `bottom:36` se sale de cuadro si no). Confetti necesita `setConfettiRun(false)+requestAnimationFrame(()=>setTrue())` para re-disparar.
- **i18n**: claves nuevas bajo `cliente.{nav,case,team,signature,tutorial}` + `design.mobile`, paridad es/en exacta (445 claves, `check:i18n` OK). Truco merge bilingüe sin romper paridad: script Node `{leaf:[es,en]}` + `pick(node,idx)` recursivo + `deepMerge`, escrito como archivo (heredoc bash rompe con acentos/«»). El script one-off va en `docs/_evidence/` (eslint-ignora docs/**), NO en `scripts/` (otro agente) — y borrar tras correr.
- **Playwright harness F2** (`docs/_evidence/f2-mobile/shoot.cjs`, playwright local 1.60): navega `/design` público, `addInitScript` set localStorage `ulp-theme` + `page.evaluate` set `data-theme` post-load, `heading.scrollIntoViewIfNeeded()` + `section.screenshot()` (NO fullPage, recorta la sección). Overlays: click trigger → `waitForTimeout` → screenshot → `keyboard.press('Escape')`. 390px mobile + light. Filtrar `/hydrat|caret-color|extension/`. 0 console errors light+dark. **Lex `<picture><source webp><img gif>` NO necesita eslint-disable de no-img-element** (el `<picture>` lo satisface); el `<img>` suelto del preview de SignaturePad SÍ lo necesita.
- **`<img>` upload preview**: `URL.createObjectURL(file)` + `<img>` con eslint-disable `@next/next/no-img-element` (es object URL local, no remoto). SignaturePad exporta PNG con `canvas.toDataURL('image/png')`; el modo upload devuelve el object URL directo.

### UsaLatinoPrime V2 — F2 Pantallas CLIENTE (DOC-51 §5-26, jun 2026)
- **Gap del módulo `cases`**: las lecturas existentes devolvían rows CRUDOS (`getCaseOverview`→CaseRow, `getCasesForClient`→CasesPage), sin enriquecer. DOC-51 referencia DTOs (`CaseWorkspaceDto`, `DocumentsMatrixDto`) que NO existían. Añadí lecturas read-only al `cases/service.ts`+`repository.ts` (autorizado: "lecturas triviales siguiendo el patrón"): `getCaseWorkspace` (caso+servicio{label/icon/color}+fase{label/desc/explainer/position}+phaseCount+partes{nombre vía person_records}+progreso vía computePhaseProgress+pendingDocs), `getDocumentsMatrix` (requirements de la fase vía `catalog.getCaseRequirements` ya existente — hace expansión per-party+overrides — unido con `case_documents` por `${reqId}:${partyId}` → estado pendiente/uploaded→revision/approved→aprobado/rejected→corregir), `getCaseMilestones` (milestones del servicio con estado derivado curso/siguiente/bloqueado/completado por phase_position vs current), `getClientDisplayName` (`client_profiles.preferred_name??first_name`). Contracts: `getTermsStatusForCase` (guard disclaimer, reusa getActiveTermsVersion+findAcceptance) + `acceptTermsFromImage` (wrap PNG→PDF + upload + acceptTermsInApp, módulo-pub para boundary-clean).
- **`catalog.getCaseRequirements` ya hace el trabajo pesado**: toma `{service_id, phase_id, parties:[{id,party_role}], requirement_overrides}` y devuelve `documents: ExpandedRequirement[]` (key, party_id, label_i18n, help_i18n, category_i18n, is_required, position) YA expandidos per-party + overrides aplicados. NO re-implementar; el `cases` lo importa dinámicamente (`await import("@/backend/modules/catalog")`) para no romper boundary cross-module.
- **`computePhaseProgress` en F2 = solo documentos** (forms/appointments llegan F3/F4): `{totalDocuments, approvedDocuments, totalForms:0, submittedForms:0, totalAppointments:0, completedAppointments:0}`. Pesos 50/30/20; devuelve 100 si todo es 0.
- **`terms_versions` repo interface DESACTUALIZADA**: la `TermsVersionRow` del repo NO tiene `title_i18n`/`body_md_i18n` (sí en database.types). Castear `row as unknown as Record<string,unknown>` para leer los i18n.
- **Boundary `app` NO puede importar `platform`** (R: app allow=[app,module-pub,frontend,shared]). El upload de firma/PDF wrap debe vivir en módulo-pub (`contracts.acceptTermsFromImage`/`signContractFromImage` hacen jpegDataUrlToPdf+createSignedUploadUrl+PUT internamente). La app action queda thin: solo `requireActor()` + clientIp + llamar módulo-pub. NUNCA importar `@/backend/platform/storage` desde `src/app/`.
- **Boundary `frontend` NO puede importar `app`** (R: frontend allow=[frontend,shared]). Los client screens NO pueden `import type {XResult} from "@/app/.../actions"`. Definir los result types EN el feature (estructuralmente idénticos al server action → pasan como prop sin error). Pasar la action como prop `(input)=>Promise<Result>`.
- **Subida segura cliente (DOC-48 §1.6)**: `startUploadAction`(→signed PUT URL) → `XMLHttpRequest` PUT con `xhr.upload.onprogress` (% REAL, no fake setInterval del prototipo) → `confirmUploadAction`(→re-lee matrix, devuelve progress+gain real). Validación cliente (PDF, ≤20MB) ANTES de subir; server re-valida. Navegar a `exito?progress=X&gain=Y` (números reales del backend, no hardcoded +14%).
- **Disclaimer signature flow**: SignaturePad exporta PNG (`getDataUrl()`); el bucket `contracts` es PDF-only (magic-byte validado). Cliente re-encoda PNG→JPEG con canvas (`toDataURL('image/jpeg',0.9)` + fondo blanco) → server `jpegDataUrlToPdf` (helper zero-dep en contracts/signature-pdf.ts o firma/pdf.ts) → upload. Tras aceptar → `camino?onboarded=1` dispara el Tutorial (~480ms).
- **IconTile/IconHalo portados a brand** (`components/brand/icon-tile.tsx`, server-safe): el prototipo usa `${color}2e` hex-alpha concat — NO sirve con tokens CSS (`var(--accent)`). Usar `color-mix(in srgb, ${color} X%, transparent)`. `IconTile`: radial bg mix(18)/mix(7) + boxShadow inset mix(15) + IconHalo opacity .55.
- **Service icon string de DB → IconName**: crear `coerceIcon(str, fallback)` que valida contra `ICON_NAMES` Set y cae a fallback. Para iconos de timeline (lucide DB names "check-circle"/"alert-circle"/"dollar-sign") mapear explícito → brand ("check"/"info"/"wallet").
- **i18n locale en RSC sin routing**: `getLocale()`/`getTimeZone()` de `next-intl/server` (leen cookies `ulp-locale`/`ulp-tz`). Resolver `{en,es}` jsonb con helper `pickLocale(value, locale)`. Cambio de idioma en config = set cookie `ulp-locale` + `window.location.reload()`. Tema/escala = lib `@/frontend/lib/theme` (`applyTheme`/`applyTextScale`, localStorage `ulp-theme`/`ulp-text-scale`, no-flash script). Claves i18n dinámicas (`section${n}.title`, `partyRole.${role}`) → castear translator `t as unknown as (k:string)=>string`.
- **Case layout guard real**: `getCaseWorkspace(actor, caseId)` en el layout (RSC) enforça membership (requireCaseAccess+RLS is_case_member); catch→notFound() (anti-enumeración). NO_CHROME (disclaimer/subir/exito) → CaseChrome client esconde nav/launcher por `pathname.endsWith()`. El layout YA no renderiza el chip `#ULP-` (cada screen trae su propio back-link).
- **Rechazo de doc = ÁMBAR jamás rojo (RF-CLI-028)**: botón "Corregir" `bg:var(--gold-deep)`, motivo texto `var(--gold-deep)`. StatusPill NO se usa para corregir (botón rojo→ámbar). Tarjeta corregir = `gold-soft`+borde gold, eyebrow "Casi listo", "Solo un detalle", pie empático "No hiciste nada mal".
- **Feature flag CTA**: `process.env.NEXT_PUBLIC_FEATURE_INTERES==="true"` (nota H-7/PS-3, lead action F3). CTA "Me interesa" presente pero `disabled` con label alterno "Disponible pronto" cuando flag off (NUNCA esconder disabled — anti-pattern).
- **Dev preview harness cliente** (`(dev)/cliente-preview/[view]`, public en middleware, `notFound()` si NODE_ENV prod): renderiza cada screen con mock props (data del prototipo María/Mateo) dentro del marco 430px. Permite Playwright sin sesión. Harness `docs/_evidence/f2-cliente/shoot.cjs` (playwright local, 390×844 mobile, set `ulp-theme` en addInitScript ANTES de hidratar + `data-theme` post-load, fullPage). 10 screenshots (5 views × light/dark), 0 console errors. Dev port puede ser 3001 si 3000 ocupado por agente paralelo.
- **Co-edición con agente paralelo (f2-admin-casos)** en mismo módulo cases/contracts: el linter/agente puede duplicar imports (vi `listCaseDocuments` dos veces) o añadir sus exports al index — verificar import block tras cada edición. Mis símbolos (getCaseWorkspace/getDocumentsMatrix/etc + getTermsStatusForCase) coexisten con los suyos (getCaseDocuments/signContractFromImage). NO tocar sus archivos; verificar SOLO los propios con grep.

### UsaLatinoPrime V2 — F2-W2-b Firma pública + Admin casos (DOC-51 §27, DOC-53 §2/§3, jun 2026)
- **Boundaries DUROS (recordar SIEMPRE)**: `app` SOLO importa `app, module-pub, frontend, shared`. NUNCA `platform/*` ni `module-int` (repository/service directos) desde `app/*`. Si una action de app necesita storage/signed-url/PDF wrap → mover esa lógica DENTRO del módulo (module-pub puede importar platform). Ej: la firma PNG→PDF+upload+sign vive en `contracts.signContractFromImage(token, jpegDataUrl, ip)` (no en la action de app). `process.env.NEXT_PUBLIC_APP_URL` se lee directo (público), no vía `platform/env` (eso es platform-import). `"use server"` files SOLO exportan async functions — nada de `export { x }` re-export de no-actions ni `void x` top-level.
- **Bucket `contracts` es PDF-only** (`ALLOWED_EXTENSIONS.contracts=["pdf"]`) y `signContract` valida magic-bytes PDF del objeto subido. El `SignaturePad` da PNG dataURL → el cliente re-encodea a JPEG (`canvas.toDataURL("image/jpeg")` con fondo blanco) y el server lo envuelve en un **PDF mínimo hecho a mano** (cero deps; `pdf-lib` prohibido). Builder: `%PDF-1.4` header + 5 objetos (catalog/pages/page/content-stream/image-XObject con `/DCTDecode`) + xref table; lee size del JPEG por marcador SOF. Funciona, pasa `validateUploadedObject`.
- **Firma `signContract(token,{signatureUploadRef,ip})` exige objeto YA subido** (no acepta dataURL). Flujo real = createSignedUploadUrl(contracts) → fetch PUT → signContract. Token single-use: se anula en el mismo update que `status=signed`. `CONTRACT_ALREADY_SIGNED` → mostrar pantalla "ya firmado" (no error). Lookup por token con service client (anon, sin RLS): `findBySigningToken` exige `status='sent' AND signing_expires_at>now()` → 404 uniforme indistinguible.
- **404 uniforme de firma = page que renderiza "enlace vencido" a HTTP 200** (NO `notFound()` que daría 404 distinguible). Anti-enumeración DOC-22 §4: misma pantalla para token inválido/expirado/consumido, CERO datos del contrato, solo "contacta a tu asesora" genérico (los contactos de `orgs.settings` son auth-gated `dashboard:view` → no leíbles anónimo; usar copy genérico).
- **API real vs brief (reconciliación)**: `cases` NO exporta `createCaseFromContract/approveDocument/rejectDocument/getCaseDocuments`. Lo real: `getCaseOverview`, `getCaseRequirements`, `getCaseWorkspace` (DTO rico: caseNumber/status/service.labelI18n/phase.index/count/progress/parties{name,role}), `getDocumentsMatrix`, `reviewDocument(actor,{documentId,verdict:'approve'|'reject',reason:{en,es},correctionDueAt})` (aprobar+rechazar COMBINADO), `getTimeline`, `onDownpaymentConfirmed`. `billing.recordManualPayment` NO existe → es `registerZellePayment(actor,{installmentId})`: al confirmar el installment `is_downpayment` emite `downpayment.confirmed`→`onDownpaymentConfirmed` activa el caso (gate de negocio payment_pending→active). Para registrar el pago manual: leer `getPaymentPlanForCase` → encontrar el downpayment installment `pending/overdue` → su `.id`.
- **Reads triviales que tuve que AGREGAR a cases** (excepción documentada del brief): `getCaseDocuments(actor,caseId)` (`listCaseDocuments` repo), `getCaseDocumentDownloadUrl(actor,documentId)` (findDocumentById→createSignedDownloadUrl `case-documents`, 5min), `listCasesAdmin(actor,{status,cursor,limit})` (enriquece cada CaseRow con clientName via `findClientDisplayName(primary_client_id)`, serviceLabel via `findServiceLite`, planKind via nuevo `findPlanKind(service_plan_id)`, phaseIndex via `listServicePhases`). `getCasesForClient` devuelve CaseRow CRUDO sin joins. **OJO trabajo paralelo**: el otro agente (cliente F2-W2-a) co-edita cases service/repository/index — `listCaseDocuments` YA existía de ellos (mi copia fue duplicado → TS2393, borrar la mía). Verificar duplicados tras agregar reads a archivos compartidos.
- **NO hay action de creación de usuario cliente** en identity/cases module-pub (el brief pide "alta inmediata usuario+perfil" pero no existe API). El modal "Nuevo caso" hace lo posible: `contracts.createContractAndSend(actor, input)` (caseId:null, planSnapshot incl. client.name/phone capturados) → devuelve `{contractId, signingToken}` → muestra link copiable. El user+case real se crea cuando exista `createCaseFromContract`/`createClientUser`. Flag `<<NEED-BACKEND>>`.
- **createContractAndSend pattern**: `sendContractForSigning` devuelve void; agregué `createContractAndSend` a contracts que crea draft→send→relee token de la row con `findContractById`→devuelve token. Útil para mostrar el signing link inmediatamente en el modal.
- **shared-case feature (primer consumidor real)** en `src/frontend/features/shared-case/`: `shared-case-view.tsx` (header+barra modo admin gold-soft+banners contextuales+tab-bar data-driven+tab activa), `build-tabs.ts` (pure fn, registry = seam para tabs futuras sin fork), `tabs/{resumen,documentos,partes}-tab.tsx`, `types.ts` (CaseWorkspaceVM + CaseDetailActions inyectadas), `ui.tsx` (SectionLabel+formatCents), `strings.ts`+`index.ts`. La RSC page arma el VM desde getCaseWorkspace+getCaseDocuments+getPaymentPlanForCase+getContractForCase+getTimeline e inyecta actions. Tab-bar pill activa `--accent-soft`+badge rojo (docs uploaded).
- **Patrón i18n pending (brief lo pide explícito)**: claves en `_pending-f2w2b.{es,en}.json` (NUEVOS, mismo árbol staff.casos.*/publicSigning.*), buildStrings importa directo de esos JSON (`import esPending from "@/frontend/i18n/messages/_pending-f2w2b.es.json"`) marcado `TODO(merge-i18n)`. NO tocar es/en.json reales (otro agente los edita en paralelo — aparecen `M` en git, no es mío). El orquestador fusiona después. Mantener paridad es/en en los pending (script Node flatten). Valido con `node -e JSON.parse`.
- **Playwright F2-W2-b** (`docs/_evidence/f2-admin-casos/shoot.cjs`): admin auth-gated → extender `(dev)/admin-preview/[view]` con views `casos`/`caso-detalle`/`firma` + `casos-mock.ts`. La firma usa tokens MÓVILES → render FUERA de `.surface-staff`. El "enlace vencido" se captura por la ruta REAL `/firma/<fake-uuid>` (HTTP 200, sin auth). Dev server: si 3000 ocupado, Next usa 3001 → apuntar `BASE_URL=http://localhost:3001`. Filtrar `/hydrat|caret-color|extension|DevTools/`. 6 screenshots, 0 console errors light+dark. `app→app` import permitido (preview importa SigningView de `(public)/firma`).
- **Resultado**: tsc 0 · eslint 0 · check:i18n OK(652) · rutas firma-fake→200, /admin/casos→307 sin sesión. Componentes reusados: brand (Card/Chip/StatusPill/GradientBtn/GhostBtn/Icon/Lex/Avatar), mobile/signature-pad, desktop (DataTable/Modal/SidePanel/EmptyState/toast). StatusPill kinds: pendiente(blue)/aprobado(green)/revision(gold)/corregir(red)/hecho(green) — `on_hold` no mapea a pill → Chip tone amber.

### UsaLatinoPrime V2 — F3 Panel de Vanessa (8 vistas, DOC-52, jun 2026)
- **Réplica del prototipo `V2/UI Vanessa`**: la SoT visual son los `view_*.jsx` + `styles.css` (los screenshots `screenshots/*.png` solo capturaron el chrome sin Material Symbols cargado — inútiles). Porté el CSS de componentes del prototipo a `src/frontend/design-system/vanessa.css` **scopeado bajo `.surface-staff`** (los TOKENS `--accent/--panel/--ink/...` ya existían en tokens.css de F1; faltaban las CLASES `.kpi/.kanban/.attend-row/.cal-grid/.funnel/.donut/.nba/.subtab/.stepper/...`). Import en globals.css tras motion.css.
- **Material Symbols Rounded** (vocabulario de iconos del prototipo, mucho más rico que el brand Icon ~55 nombres): componente `MSym` (ligadura `material-symbols-rounded` + `fontVariationSettings FILL/wght/opsz`) + `MaterialSymbolsFont` (`<link>` Google Fonts variable axes). Cargar en `(panel)/layout.tsx` (prod) y en la preview page (dev). DOC-52 §0.1 lo manda explícito.
- **date-fns-tz dual-hour** (`src/frontend/lib/datetime.ts`, NUEVO): `fmtTime/fmtTimeZoned/fmtDateFull/fmtHeaderDate/fmtRelative/dualHour/tzLabel/tzAbbrev`. `dualHour(instant, staffTz, clientTz, locale)` → `{primary, secondary}` con `formatInTimeZone` sobre el MISMO instante UTC (NUNCA offsets fijos). Utah va **−2h** de Florida (el prototipo lo invierte — P1 DOC-23). Chip estático = "Florida (ET)" genérico; horas concretas con `zzz` (EST/EDT real por instante).
- **Patrón inject-actions estricto (boundaries)**: `app/.../ventas/actions.ts` = thin `"use server"` que envuelve `kanban`/`scheduling` index (`moveCard/createLead/createLeadCategory/updateLead/toggleTaskDone/bookAppointment/createProspectAppointment/complete/cancel/markNoShow/saveAvailabilityRules/addException/removeException/updateSchedulingSettings/liftRebookingBlock`). NUNCA repository/platform desde app. `setLocaleAction` solo setea cookie `ulp-locale` (no importa platform/env).
- **Signaturas reales que diferían del brief**: `BookAppointmentInput` usa `startsAtUtc:Date` + `kind:'video'|'phone'|'presencial'` + `notes` (no `apptType/startsAtIso/modality/note`); `BookingWarning.code` ∈ `OUTSIDE_AVAILABILITY|OUTSIDE_WINDOW|SLOT_CONFLICT` (overlap = SLOT_CONFLICT/SLOT_TAKEN). `updateSchedulingSettings` usa `minNoticeHours/rebookingPenaltyDays` (no `noShowPenaltyDays`). `liftRebookingBlock(actor, caseId:string)`. `CreateLeadResult` = `{type:'lead'}|{type:'warning', exactMatches, weakMatches}`. `createLead` input usa `fullName/interestedServiceId/note` (no name/serviceId). LeadRow/TaskRow = tablas DB snake_case (`full_name/phone_e164/created_at/source/contacted_at/lost_reason/category_id`; tasks: `done_at` null=pendiente, `text/tag`).
- **Kanban cards genéricos**: `kanban_cards.ref_id` → lead (no FK directo). Hidratar board = `getBoard({kind:'leads'})` + `listLeads({})` + Map por id, joinear card.ref_id. Columnas sort by `position`; `is_terminal_won/lost`.
- **Drag&drop HTML5 nativo** (sin @dnd-kit, no instalado): `draggable` + `onDragStart/onDragOver(preventDefault)/onDrop` con clase `.dragging`/`.dragover`, optimistic move + revert on error. Suficiente y ligero (brief lo pide). Won → LexBubble oferta "Crear caso"; Lost → modal motivo obligatorio.
- **Clientes reusa `shared-case`** (F2): el workspace `/ventas/clientes/[caseId]` ES el mismo patrón EXACTO que `admin/casos/[caseId]` (getCaseWorkspace+getCaseDocuments+getPaymentPlanForCase+getContractForCase+getTimeline → CaseWorkspaceVM, inyecta `reviewDocument/registerPayment/resendSigningLink/getDocumentUrl` de `admin/casos/actions`, `backHref="/ventas/clientes"`, `isAdmin=false`). Tabs Cartas/Traspaso = F4 ("Pronto"). El modal "Nuevo caso" = `NewCaseModal` de admin reusado.
- **LexPrefsProvider** (`shared/lex-prefs.tsx`): context localStorage `ulp-lex-bubbles` que apaga TODAS las burbujas (RF-VAN-005 CA3). Cada page envuelve su vista en `<LexPrefsProvider>`. Las vistas leen `useLexPrefs().bubbles` para `enabled`. LexDock FAB 66px orb + panel glass; LexBubble glass `dangerouslySetInnerHTML` (texto compuesto server-side de i18n, sin user input). Lex V2.0 = determinista (sin LLM).
- **i18n inyección bilingüe**: script `docs/_evidence/f3-vanessa/inject-i18n.mjs` (eslint-ignora docs/**) con `{leaf:[es,en]}` + `pick(node,idx)` + `deepMerge` → escribe es.json+en.json en un pase. `staff.ventas.*` (miDia/leads/newLead/citas/nuevaCita/disponibilidad/clientes/metricas/config/lex/preview) + nav items. Markup en RSC con `t.markup("k",{b:(c)=>`<b>${c}</b>`, var})` para `<b>` en burbujas Lex. 1050 claves, paridad OK. Las vistas client NO importan `t` — reciben `strings` resueltos como prop.
- **Dev preview** (`(dev)/ventas-preview/[view]`, public en middleware prefix `/ventas-preview`, `notFound()` si NODE_ENV prod o `ENABLE_VENTAS_PREVIEW!==true`): renderiza las 8 vistas con mock (`mock.ts` shapes de data.js) + actions inertes `async()=>({ok:true})`. **OJO env**: `>> .env.local` sin newline pega la var a la anterior — verificar con tail; perl fix. Reiniciar dev server tras cambiar .env.local (no HMR). Harness `shoot.cjs`: 1440×960, `addInitScript` set `ulp-theme` + `data-theme` post-load, `networkidle`+900ms, fullPage, filtro `/hydrat|caret-color|extension|DevTools/`. 14 screenshots (7 vistas × light/dark), 0 console errors.
- **Resultado F3 (8 vistas)**: tsc 0 · eslint 0 · check:i18n OK(1050) · 8 rutas /ventas/*→307 sin sesión (NO 500) · 7 preview→200. Mi día (4 KPIs+hot flag+por atender+agenda+tareas+Lex), Leads (kanban 7 cols HTML5 DnD+modal nuevo lead/caso), Citas (CalendarGrid+SidePanel detalle+modal dual-hour), Disponibilidad (grid recurrencia+settings+excepciones+lift block), Clientes (lista+workspace shared-case), Métricas (embudo+barras+donuts SVG+fuentes, sin recharts), Configuración (tema/acento/lex/idioma). Pages prod thin (reads disponibles + em-dash donde falta) — hidratación completa de agregados/board es iterativa.

### UsaLatinoPrime V2 — F3 Pantallas CLIENTE agendar/cita (DOC-51 §18-19, jun 2026)
- **2 pantallas**: `agendar/page.tsx`+`agendar/actions.ts` (API-SCH-01 getSlots, API-SCH-02 book) + `cita/[appointmentId]/page.tsx`+`actions.ts` (API-SCH-03 cancel, API-SCH-04 reschedule). Features en `src/frontend/features/cliente/{agendar,cita}/`. La ruta canónica de la pestaña "Citas" del CaseNav es `/caso/[caseId]/agendar` (NO `/citas` ni `/cita-confirmada` — esos nombres del prototipo NO existen como rutas). `agendar/page.tsx` ES el resolver §0.1: si hay appointment `scheduled` → `redirect(/cita/[id])`, salvo `?reschedule=[id]` (reagendar reusa el calendario). Actualicé el match del bottom-nav "citas" de `cita-confirmada`→`cita` y el tile "Próxima cita" de camino-screen de `/citas`→`/agendar` (apuntaba a ruta inexistente → 404).
- **Firmas reales scheduling (cliente)**: `getAvailableSlots(actor,{caseId,windowFromUtc:Date,windowToUtc:Date})→{slots:Slot[{startUtc,endUtc:Date}],durationMinutes,kind,sequenceNumber,staffId,staffTimezone}`. `bookAppointment(actor,{caseId,startsAtUtc:Date,reminder1d,reminder1h,notes})→{appointment?:AppointmentRow, warnings?}` (cliente nunca recibe warnings — esa rama es staff). `cancelAppointment(actor,{appointmentId,reason})→void`. `rescheduleAppointment(actor,{appointmentId,newStartsAtUtc:Date,reminder1d?,reminder1h?})→AppointmentRow`. `getAppointmentForClient(actor,id)`/`getCaseAppointments(actor,caseId)`→AppointmentRow CRUDO (snake_case, sin nombre del staff). `SchedulingError.code` union + `.meta?.blockedUntil` (NO `.details`). REBOOKING_BLOCKED → pantalla bloqueo con fecha de desbloqueo (`fmtDateFull`); NO_APPOINTMENTS_LEFT/CASE_NOT_ACTIVE → EmptyCase; SLOT_TAKEN en book → refetch grid del mes; OUTSIDE_WINDOW en reschedule (<24h) → toast ámbar.
- **Hora dual CLIENTE (§6.5)**: agregué `clientDualHour(instant, clientTz, staffTz, locale, staffCity?)→{primary,secondary}` a `datetime.ts` (el `dualHour` existente es staff-primary). Cliente: hora LOCAL grande + "{hora} en Utah" pequeña, ambas de `formatInTimeZone` del MISMO instante UTC. Utah=`America/Denver` va −2h de Florida (2:00 PM ET = 12:00 PM MT — verificado en screenshots). **La TZ del cliente que mira = `getTimeZone()` de next-intl/server** (cookie `ulp-tz`, espejo de users.timezone), NO la del staff. La TZ de oficina para la cita confirmada = constante `OFFICE_TZ="America/Denver"` (hecho de negocio estable; no hay read client-safe de staff TZ por cita).
- **GAP advisor name `<<NEED-BACKEND>>`**: NINGÚN module-pub expone el display_name del asesor a una sesión cliente. `getCurrentStaffProfile` solo lee el actor staff; `org.getOrgConfig`/`identity` staff reads son `can(dashboard,view)` admin-gated; `getStaffProfileById` existe en identity/repository pero NO exportado del index (y exportarlo = tocar backend, prohibido por brief). Render del asesor = genérico "Tu asesora legal" + avatar neutro "•". Flag para backend: read client-safe `getCaseAdvisor(actor,caseId)→{displayName,avatarUrl}` (RLS is_case_member).
- **Boundary R5 recordado (otra vez)**: `frontend` NO importa `app` ni para types. Los result types de las actions (`SlotWire/GetSlotsActionResult/BookAppointmentActionResult/RescheduleAppointmentActionResult/CancelAppointmentActionResult`) se DEFINEN en el feature (estructuralmente idénticos) y las actions de app se pasan como props — pasan type-structurally. eslint `boundaries/element-types` lo caza si importás `@/app/...` desde `frontend/`.
- **Calendario vanilla (sin libs)**: grid 6-semanas Sunday-first ("D L M M J V S"), solo días con slots reales tocables (`slotsByDay` Map por `dayKeyInTz` en TZ cliente). Navegación de mes refetch via getSlotsAction con `monthWindowUtc` (1-día padding). Slots 2-col 58px, hora primary grande + secondary "en Utah". Elegir otro día limpia el slot. CTA muta "Elige un horario"(disabled)→"Confirmar cita"(slot)/"Cambiar a este horario"(reschedule). Checkboxes 28px verde (role=checkbox aria-checked). Skeleton slots = `animation:shimmer 1.4s` inline (NO clase `.anim-shimmer`, no existe; el keyframe `shimmer` sí). `--card-alt` token existe directo.
- **Cita confirmada**: tarjeta verde gradiente `green-soft→card` borde `green 13%`, check disc `checkPop .6s`, filas Fecha/Hora/Con (Con=avatar gradiente navy en vez de IconTile). Botón "Entrar a la videollamada · Pronto" VISIBLE pero `disabled` (LiveKit=F7) + nota — nunca esconder disabled. Cancelar = BottomSheet confirmación (recuerda 7 días, nunca 1-tap; botón rojo) → `router.replace(/agendar)+refresh()`. Cambiar cita = link a `/agendar?reschedule=[id]`. Estado completed = chip verde "Completada" + Lex calma (sin confetti) + nota staff en panel verde. Confetti+playChime al `?nueva=1` (reusa componente F2). `--green-deep` NO existe → fallback `var(--green-deep, var(--green))`.
- **Playwright** (`docs/_evidence/f3-cliente-agendar/shoot.cjs`, 390×844 mobile): preview `(dev)/cliente-preview/[view]` con views agendar/cita/cita-completada/agendar-bloqueado/agendar-vacio + mocks (slots UTC reales via `fromZonedTime` de wall-times Florida → render "2:00 PM"/"12:00 PM en Utah"). Harness clickea día disponible + slot "2:00 PM" para capturar CTA activo. 10 screenshots, 0 console errors light+dark. La "N" bottom-left = Next dev indicator (artefacto). Rutas live `/caso/demo/agendar` y `/cita/x`→307 /welcome sin sesión. Gates: tsc 0, eslint 0, check:i18n OK(1128), vitest 488/488. i18n bajo `cliente.{agendar,citaConfirmada}` paridad es/en via script `{leaf:[es,en]}`+`pick` (borrado tras correr).

### UsaLatinoPrime V2 — F4-Ola2 Editor de formularios + datasets + costos IA (DOC-53 §5/§6, jun 2026)
- **Editor de formularios = la pantalla más compleja**. Feature en `src/frontend/features/admin/form-editor/`: `form-editor-view` (shell+breadcrumb+kind chip→PdfMode|AiLetterMode), `pdf-mode` (4-stage bar Subir→Estructurar→Previsualizar→Publicar + version chips v1/v2/v3 + banner inmutabilidad), `structure-editor` (2 paneles: izq acordeón grupos+question-card / der PdfViewer), `question-card` (expandible: Redacción+Ayuda **ES|EN lado a lado** via `I18nField`, tipo, options select, mapeo PDF combobox, **origen segmented 4 opc** cliente/extracción/generación/perfil — perfil PII `🔒`+nota "se resuelve localmente"), `pdf-viewer` (pdfjs), `upload-stage` (dropzone+resumen por tipo/página), `preview-stage` (marco móvil 390px + ClientWizardPreview fiel + Generar PDF prueba→iframe), `publish-stage` (checklist blocking rojo/warning dorado+ack checkbox→banner verde), `ai-letter-mode` (2 col config|prueba), `build-vm` (mapper puro RawFormEditorData→VM), `types`+`strings`+`index`. RSC page inyecta actions; `?v=<id>` recarga la versión elegida.
- **pdfjs-dist 4.10.38** (NO la 6.x — la 4.x es estable con Next 15/Turbopack sin polyfills). Import dinámico en `"use client"`: `const pdfjs = await import("pdfjs-dist"); const workerSrc = (await import("pdfjs-dist/build/pdf.worker.min.mjs?url")).default; pdfjs.GlobalWorkerOptions.workerSrc = workerSrc;`. El `?url` necesita ambient `declare module "*?url"{const url:string;export default url}` (creé `url-imports.d.ts` en la feature, `**/*.ts` del tsconfig lo incluye). Overlays de campos: rect mupdf `[x0,y0,x1,y1]` en puntos PDF (origen abajo-izq) → escalar por RENDER_SCALE(1.35) + flip Y `top=pageHeight-y1*scale`. 3 estados: mapeado `color-mix(--accent 30%)`, sin-mapear `--gold-soft`+dashed, seleccionado `--accent` sólido. Filtrar `pdf.worker|Setting up fake worker|workerSrc` en el harness console.
- **Backend ya completo (6 stubs F4-Ola2)** — NO tocar lógica. Lo que SÍ agregué (autorizado: "reads triviales + server actions de app que envuelven module-pub"): a `catalog/service.ts` reads `getFormEditorData(actor,formId,openVersionId?)` (form+service+versions+openVersion tree+sources{documents con paths del extraction_schema,forms,profileFields}+generationConfig), `getVersionPdfUrl` (signed download catalog-assets — el viewer pdfjs lo consume), `createFormPdfUploadUrl`/`createDatasetFileUploadUrl` (signed upload; **app NO importa platform/storage** → el helper vive en el módulo, `import("@/backend/platform/storage")` dinámico), `listDatasetsAdmin`/`getDatasetDetail` (enriquecidos item_count/total_tokens/used_by). A `repository.ts`: `listDatasetItems`/`countDatasetUsage`/`listDatasetUsage`. A `catalog/actions.ts` (module-pub): wrappers de los 6 stubs (`createAutomationVersionAction`/`redetectFieldsAction`/`aiProposeStructureAction`/`generateTestPdfAction`/`proposeExtractionSchemaAction`/`testGenerationAction`) + `getVersionPdfUrlAction`+`createFormPdfUploadUrlAction`+`updateDatasetItemAction`/`deleteDatasetAction`/`createDatasetFileUploadUrlAction`. Exportar reads+types nuevos en `index.ts`.
- **generateTestPdf devuelve `Uint8Array`** → el action lo serializa a base64 (`Buffer.from(bytes).toString("base64")`); el cliente lo reconstruye `Uint8Array.from(atob(b64),c=>c.charCodeAt(0))`+`new Blob([],{type:"application/pdf"})`→`URL.createObjectURL`→`<iframe>`. NO se persiste (mismo motor mupdf que prod).
- **Selector de MODELO**: `src/shared/constants/ai-models.ts` ya tenía la lista cerrada de Henry (`GENERATION_MODELS`=[sonnet-4-6, opus-4-7, fable-5, haiku-4-5], `DEFAULT_GENERATION_MODEL`="claude-sonnet-4-6"). El prompt decía fable-5 — IGNORAR, usar la constante. `<select>` sobre `GENERATION_MODELS`, nunca input libre. El backend `updateGenerationConfig` valida contra `GENERATION_MODELS` (CATALOG_MODEL_NOT_ALLOWED).
- **Datasets** (`features/admin/datasets/`): `datasets-list-view` (banner PII gold fijo + DataTable name/purpose/source-chip/items/tokens-compact/used-by-clicable/switch + modal Nuevo dataset) + `dataset-detail-view` (header chips + PillTabs Ítems·Usos + filtros jurisdicción/outcome + tabla con tokens "—"+tooltip si null + SidePanel Nuevo ítem con PillTabs excluyentes Pegar/Subir, bloqueo si ambos vacíos). Source chips: eoir→blue, uscis→green, public_court→amber, manual→gold. Outcome: granted→green "Concedido", denied→red "Denegado".
- **Costos IA** (`features/admin/ai-costs/`, `/admin/ai-costs` + nav item `aiCosts` en `lib/nav.ts` grupo finance module `metrics`): `getCostsSummary` HOY solo da `{totalUsd,bySource,byMonth}`. Construí KPIs+budget bar (80% gold/100% red)+por-fuente(Claude/Gemini)+por-mes. El **ranking, failureRate, totalTokens, split is_test (RF-ADM-037) requieren agregado más rico** → sección scaffold + TODO + flag `<<NEED-BACKEND>>` (extender el read ai-engine). NO expandí el query (sería backend no-trivial, fuera de "reads triviales").
- **Wizard catálogo paso 5 (forms)**: era stub F4 (Lex señala) → lo actualicé para listar `phases.flatMap(forms)` como cards link a `/admin/catalogo/{serviceId}/formularios/{formId}` (chip "PDF oficial" azul / "Generación IA" dorado). Mantiene el stub Lex cuando 0 forms.
- **Props de componentes (verificar SIEMPRE antes de usar)**: `DataTable` Column = `{id, header, cell}` (NO key/render). `Modal`/`SidePanel` = `{open,onOpenChange,title,footer,width}`. `EmptyState` = `{title,subtitle,mood,action:{label,onClick}}`. `Kpi` usa `hot:boolean` (NO variant). `Chip tone` ∈ blue/gold/green/amber/red (NO "neutral" — usar span `--chip` propio). `Lex size` ∈ 56/62/78/92/120/130/166/186 (NO 96/110). `GradientBtn`/`GhostBtn` default `full=true` → pasar `full={false} size="md"` para botones inline (si no, ocupan 100% del flex row). `ProgressBar` = `pct` (NO value/indeterminate — para indeterminado usar div+`animation:shimmer 1.4s`). Icon names válidos: `chevR/chevD/chevL` (NO chevron-right), `gear` (NO settings), `route`/`grip`→route, `x` (NO trash), `dollar`/`wallet`/`bolt`/`sparkle`/`form`/`doc`/`user`/`shield`.
- **Callbacks `Promise<void>`**: si un onCreate/onChange devuelve `Promise<void>`, NO hacer `return toast.error(...)` (toast devuelve valor → TS2322). Usar `{ toast.error(...); return; }`.
- **Dev preview F4** (`(dev)/admin-preview/[view]` + views form-editor-pdf/form-editor-ai/datasets/dataset-detalle/ai-costs en `page.tsx` VIEWS[] + `preview-client.tsx` branches + `f4-mock.ts` data I-360 de §4). Env `ENABLE_ADMIN_PREVIEW=true` en `.env.local` (append con `\n` leading, el último var no tenía newline). Harness `docs/_evidence/f4-editor/shoot.cjs` (1440×960, set ulp-theme antes de hidratar + data-theme post-load, fullPage, filtro pdf-worker) → 10 screenshots light+dark 0 errors + `shoot-expanded.cjs` (clic en pregunta para capturar el origin selector). El mock `getPdfUrl→null` deja el visor con placeholder (no hay PDF real en preview — es correcto).
- **Resultado**: tsc 0 · eslint 0 (--max-warnings=0) · check:i18n OK(1139) · build ✓ · backend vitest 691/691 · rutas live `/admin/{catalogo/.../formularios/...,datasets,datasets/x,ai-costs}`→307 sin sesión (NO 500). 10 criterios de PROMPT-ADM-05 verificados en screenshots.

### UsaLatinoPrime V2 — F4-Ola3 FormWizard MOTOR COMPARTIDO (DOC-50 §6, SOT-3, jun 2026)
- **Motor único `src/frontend/features/form-wizard/`** (SOT-3: segunda pieza compartida tras shared-case): `types.ts` (WizardForm/WizardGroup/WizardQuestion/WizardLabels + action fn types — RE-DECLARADAS porque frontend NO importa backend/app; matchean structurally al DTO pasado por prop), `build-question-schema.ts` (Zod GENERADO `buildQuestionSchema(q)→z.ZodTypeAny` + pure `validateQuestion/validateGroup/firstInvalidGroupIndex` que MIRROREAN exacto el server `validateAnswerTypes` de domain.ts — códigos required/regex/min/max/type, mismo short-circuit), `use-autosave.ts` (debounce 1.5s + flush en blur/step-change/visibilitychange/pagehide/unmount + retry backoff 2s→5s→15s + serializa saves con inFlightRef), `autosave-queue.ts` (IndexedDB `ulp-form-wizard`/`drafts`, merge por (caseId:formId:partyId), best-effort, degrada silencioso si no hay IDB), `use-dictation.ts` (Web Speech `SpeechRecognition||webkit`, lang es-US/en-US, append en vivo, audio NUNCA se guarda, `isSupported` check → textarea sigue funcionando), `fields.tsx` (6 controles por field_type + PrefillChip "Ya lo tenemos" gold+sparkle / "Lo cambiaste tú" blue al editar + dictation 84px con 5 barras `wave 0.5s i*0.1s alternate` + `anim-ring-pulse`), `resolve.ts` (pickI18n/buildInitialAnswers/coerceInitialValue/isReadOnly puros), `form-wizard.tsx` (orquestador: header back+title+contador gold+ProgressBar h8 + autosave indicator + Lex opcional + step nav con soft-block + read-only branch + submit→Confetti+playChime→success), `labels.ts` (resolveWizardLabels(t) desde next-intl, engine desacoplado).
- **DTO enrichment trivial (autorizado)**: `getFormForClient` NO devolvía `validation`/`labelI18n`/`kind`/`isPerParty` → agregué los 4 al `FormForClientDto`+`FormQuestionDto` (validation jsonb `{regex,min,max}` para Zod local; `findFormDefinitionById` select +`label_i18n`). NUEVO read `getClientFormsForCase(actor,caseId)` (lista forms client-facing de la fase actual: `catalog.listFormDefinitions(phaseId)` filtrado `filled_by∈{client,both}`, expansión per-party con `findFormResponse`+`resolvePartyName`, status para pill). Exporté `listFormDefinitions` del catalog index (wrapper service sobre repo). `form_questions.validation` YA existe en db.types (`Json|null`); el repo `select("*")` ya lo trae.
- **3 rutas cliente**: `formulario/[formId]/page.tsx` (RSC→getFormForClient→FormularioScreen wrapper que inyecta router+actions al motor; states: not-found/no-version→EmptyCase, completado→read-only branch del motor), `formularios/page.tsx` (lista: 0→empty, 1→redirect al wizard/historia, 2+→FormulariosList cards con pill Borrador/Enviado+per-party), `historia/page.tsx` (resuelve el ai_letter de la fase vía getClientFormsForCase→getFormForClient→HistoriaScreen con `withLex`+chip "Te escucho con atención"+dictation per-textarea; placeholder amable si no hay form). Actions thin `"use server"` en formulario/historia/actions.ts (wrappers de saveFormDraft/submitFormResponse, ambos sin per-route diferencia → mismo motor).
- **CABLÉ el preview admin al motor (drop-in SOT-3 logrado)**: `preview-stage.tsx` reemplazó su `ClientWizardPreview`/`FieldPreview` local por `<FormWizard>` real vía `wizard-bridge.ts` (mapea QuestionGroupVM/QuestionVM→WizardForm; `prefillValue:"—"` placeholder para que el chip "Ya lo tenemos" muestre en preview; status:null→nunca read-only; actions no-op). El TODO(Ola-3) quedó RESUELTO. `I18nValue` del editor viene de `../shared/i18n-field` (NO de `./types`). El preview ahora renderiza el MISMO motor que la app cliente (DOC-53 §5.1.3 "render real").
- **Microcopy textual de prompts** (cliente/20+21): contador "Paso {n} de {total}" gold, prefill "Ya lo tenemos"+"lo tomamos de tu acta de nacimiento", validación AMABLE sin rojo agresivo ("Esto nos hace falta para continuar." en gold-deep no red), dictation reposo azul "Tocar para hablar" / grabando rojo "Escuchando… toca para parar". i18n bajo `cliente.formWizard.*` (29 keys) + `cliente.formularios.*` + `cliente.historia.listeningChip`, paridad es/en via script `{leaf:[es,en]}`+deepMergePick (docs/_evidence eslint-ignored, NO heredoc — rompe con «»/acentos; escribir como .mjs con Write). 1182 keys check:i18n OK.
- **Playwright F4-wizard** (`docs/_evidence/f4-wizard/shoot.cjs` mobile 390×844 + `shoot-admin.cjs` desktop 1440×960): views nuevas en `(dev)/cliente-preview/[view]` (formulario/formulario-prefill/historia/formulario-enviado) con mock RICO en `wizard-mock.ts` (NO en mock.ts — heredoc rompe; archivo separado con Write) cubriendo los 6 field types + prefill + Mi Historia + submitted. El motor se monta DIRECTO con actions no-op (los wrappers cliente necesitan router; en preview navegarían). Admin: navega a form-editor-pdf, click "Previsualizar", screenshot del wizard en marco staff 390px. 12 screenshots (8 cliente light+dark + 4 admin), 0 console errors. Filtro `/hydrat|caret-color|extension|DevTools|pdf.worker|fake worker/`. **OJO**: Web Speech SÍ está disponible en Chromium → el botón dictado sale azul "Tocar para hablar" (no unsupported). El "N" bottom-left = Next dev indicator (artefacto).
- **Props verificados**: ProgressBar=`pct`+`height`. Chip tone∈blue/gold/green/amber/red+`dot`. IconTile=`name` (NO icon)+`color`+`size`. Icon `mic`/`sparkle`/`arrowL`/`chevR`/`shield`/`check`/`clock`/`info` existen. Lex mood∈calma/feliz/atento/señala/celebra, size∈56/62/78/92/120/130/166/186 (130 para success, 56 para Lex listening, 92 read-only). Confetti=`run` boolean + `playChime()`. GradientBtn/GhostBtn default full=true → `full={false}` para inline. zod v4.4.3 instalado (`z.coerce.number`, `z.literal(true,{message})`, `z.preprocess`).
- **Resultado F4-3**: tsc 0 · eslint 0 · check:i18n OK(1182) · vitest 795/795 (782 backend + 13 wizard nuevos en `form-wizard/__tests__/build-question-schema.test.ts`) · build ✓ · 3 rutas live→307 sin sesión. Motor + 3 pantallas + Mi Historia + preview admin cableado. First Load wizard ~219KB (<300KB RNF-035).
